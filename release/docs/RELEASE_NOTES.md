# Release Notes

## 0.1.0 source release

- 提供 Windows 桌面版和浏览器版源码启动器，桌面模式默认启用 Chromium GPU 加速。
- 支持 JSON、CSV、TXT 导入、目录扫描、会话归档、严格时间范围和最后聊天时间范围。
- 超长会话按动态字符预算连续分段，保留有限 overlap，并覆盖所选范围内的全部核心记录。
- 支持任务候选审核、磨合反馈、人物证据、任务图、行程、开放地图标记和任务建议。
- 所有发布版可变文件按 `data/`、`logs/`、`assets/img/` 分类。
- 包含虚构示例会话、通用默认头像和中性背景，不含个人存档、模型配置、密钥、历史日志、下载头像或浏览器数据。
- 文档包括用户手册、开发者架构、聊天导出格式、故障排查和隐私数据说明。

这是 `0.1.0` 源码预览包。首次使用需安装 Node.js 22.12 或更高版本（推荐 24 LTS），并在 `app` 目录执行 `npm install`。当前未附带签名安装程序、自动更新器、数据库加密或正式跨版本迁移保证。
